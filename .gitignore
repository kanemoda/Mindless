# Rust build artifacts (rebuilt locally with `cargo build`)
/target

# Match runner built from source per TESTING.md; not stored in the repo
/tools/bin/fastchess

# fastchess tournament auto-save written to the repo root during a match
/config.json

# NNUE training data (gigabytes; regenerate with `mindless datagen`)
/data
/tools/nnue/data
*.txt.part*

# NNUE trainer build artifacts and bullet checkpoints.
# The final trained net under tools/nnue/nets/ IS committed; these are not.
/tools/nnue/trainer/target
/tools/nnue/checkpoints
*.bulletdata
